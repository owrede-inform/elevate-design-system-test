import { TabPanelComponent } from '../components/tabs/tab-panel/tab-panel.component.js';
/**
 * A tab panel component that wraps the content of a single tab
 * inside a tab group.
 *
 * The component will only render correctly if used in conjunction with `elvt-tab-group`.
 *
 * @since 0.0.1
 * @status Complete
 *
 * @slot - The tab's content.
 */
export declare const ElvtTabPanel: import("@lit/react").ReactWebComponent<TabPanelComponent, {}>;
