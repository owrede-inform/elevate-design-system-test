# ELEVATE Design System - Package Manager Configuration
# This project uses yarn as the package manager
# Configure npm to work with GitHub Packages for @inform-elevate scope

@inform-elevate:registry=https://npm.pkg.github.com

# Prevent accidental npm usage - this project uses yarn
engine-strict=true